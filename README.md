# Only for Our Group

本项目是我们小组的学习项目，仅限组内使用。

## Unity3D 扩展

目前仅对以下文件进行了修改：

- `renders/render_1v1.py`
- `env_base` 相关文件

新增功能：

- 增加了 `render(mode="unity3d")` 渲染模式

## 运行说明

运行前请注意以下几点：

1. 需要提前创建 `result` 文件夹。
2. `renders/render_1v1.py` 中包含对文件路径的要求，使用前可能需要根据本地环境进行修改。

## 运行命令

```bash
python -m renders.render_1v1

